<header>
  <h1><%= t :brand %></h1>
  <p><%= t :tagline %></p>
</header>

<nav>
  <% t('home.nav').each do |_, name| %>
    <%= link_to name, anchor: name.downcase %>
  <% end %>
</nav>

<div id='what'>
  <h2><%= t 'home.what.heading' %></h2>

  <%= render_markdown t('home.what.description') %>
</div>
